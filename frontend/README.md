# PDF Page Extractor Frontend

React + TypeScript client for uploading a PDF, previewing pages, selecting pages, rearranging the output order, and downloading the generated PDF.

## Run

```bash
npm install
npm run dev
```

Create `.env` from `.env.example` if the backend URL changes.
